# Types + Migration Menu Patch

Type encodings and the DuckDB→DuckLake migrator updates are **edit-in-place**
on existing stable docs pages:

- `docs/stable/specification/data_types.md`
- `docs/stable/duckdb/migrations/duckdb_to_ducklake.md`
- `docs/stable/duckdb/unsupported_features.md`

**No new menu entry** is required in `_data/menu_docs_stable.json`.

(Branching still needs the Guides menu insert documented in
[`_data/MENU_PATCH.md`](_data/MENU_PATCH.md).)
